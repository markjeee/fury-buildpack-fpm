#!/usr/bin/env bash

export LANGUAGE=en_US.UTF-8
export LC_ALL=en_US.UTF-8
export LANG=en_US.UTF-8

if [ -f "$1"/pkg/*.deb ]; then
    package=($1/pkg/*.deb)
else
    exit 1
fi

cat << EOF
---
package: $package
kind: deb
EOF
