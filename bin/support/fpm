#!/usr/bin/env bash

fpm_path=$(which fpm)
if [ -n "$fpm_path" ]; then
  fpm "$@"
else
  buildpack_fpm_dir=$(cd "$(dirname $0)/../.."; pwd)
  echo "Executing fpm from: $buildpack_fpm_dir"

  BUNDLE_GEMFILE="$buildpack_fpm_dir/Gemfile"
  bundle exec fpm "$@"
fi
